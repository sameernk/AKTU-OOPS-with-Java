//Question- Write a Java program that prompts the user for a character, converts it to its ASCII value, and displays the result.

package q22546;
import java.util.Scanner;
public class CharCodeQuest {
  public static void main(String[] args) {
    Scanner scanner = new Scanner(System.in);
    char magicalChar = scanner.next().charAt(0);
  
  int c=(int)magica1Char;
    System.out.println(c);
    Scanner.close();
  }
}
